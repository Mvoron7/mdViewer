<Window x:Class="mdViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mdViewer"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="500" Width="825" FontSize="16" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Sourse" Grid.Row="0"/>
        <TextBox x:Name="Source" Margin="0 10" TextChanged="Source_TextChanged" Grid.Row="0" Grid.Column="1"/>
        <Label Content="Style" Grid.Row="1"/>
        <TextBox x:Name="FileOfStyle" Margin="0 10" TextChanged="Style_TextChanged" Grid.Row="1" Grid.Column="1"/>
        <WebBrowser x:Name="brow" Grid.Row="2" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
